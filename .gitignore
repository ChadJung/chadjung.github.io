# Logs
logs
*.log
npm-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
*.tsbuildinfo

# Editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea

# Project local data / working files (not for public repo)
# NOTE: /data/ is anchored to root — it must NOT match src/data/.
/data/
/JD.txt
/portfolio_build_prompt.md
# Tool dirs — may appear at root or nested, so left unanchored.
.moai/
.claude/
